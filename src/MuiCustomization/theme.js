import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#e1fdf8", // your custom primary color
    },
    secondary: {
      main: "#07594b", // your custom secondary color
    },
    accent: {
      main: "#ff6f00",
    },
  },
});

export default theme;
